---
title: "Customize keyboard shortcuts"
titleKo: "키보드 단축키 커스터마이즈"
description: "Customize keyboard shortcuts in Claude Code with a keybindings configuration file."
descriptionKo: "키바인딩 설정 파일로 Claude Code의 키보드 단축키를 커스터마이즈합니다."
category: "settings"
sourceUrl: "https://code.claude.com/docs/en/keybindings.md"
fetchedDate: "2026-02-19"
---

# Source: https://code.claude.com/docs/en/keybindings.md

# 키보드 단축키 커스터마이즈

> 키바인딩 설정 파일로 Claude Code의 키보드 단축키를 커스터마이즈합니다.

Claude Code는 커스터마이즈 가능한 키보드 단축키를 지원합니다. `/keybindings`를 실행하면 `~/.claude/keybindings.json`에 구성 파일을 만들거나 열 수 있습니다.

## 구성 파일

키바인딩 구성 파일은 `bindings` 배열이 있는 객체입니다. 각 블록은 컨텍스트와 키 입력에서 동작으로의 맵을 지정합니다.

> **참고**: 키바인딩 파일의 변경 사항은 Claude Code를 재시작하지 않아도 자동으로 감지되어 적용됩니다.

| 필드 | 설명 |
| :--------- | :------------------------------------------------- |
| `$schema` | 에디터 자동 완성을 위한 선택적 JSON Schema URL |
| `$docs` | 선택적 문서 URL |
| `bindings` | 컨텍스트별 바인딩 블록 배열 |

이 예시는 chat 컨텍스트에서 `Ctrl+E`를 외부 에디터를 열도록 바인딩하고 `Ctrl+U`를 바인딩 해제합니다:

```json
{
  "$schema": "https://www.schemastore.org/claude-code-keybindings.json",
  "$docs": "https://code.claude.com/docs/en/keybindings",
  "bindings": [
    {
      "context": "Chat",
      "bindings": {
        "ctrl+e": "chat:externalEditor",
        "ctrl+u": null
      }
    }
  ]
}
```

## 컨텍스트

각 바인딩 블록은 바인딩이 적용되는 **컨텍스트**를 지정합니다:

| 컨텍스트 | 설명 |
| :---------------- | :----------------------------------------------- |
| `Global` | 앱 전체에 적용 |
| `Chat` | 메인 채팅 입력 영역 |
| `Autocomplete` | 자동 완성 메뉴가 열린 경우 |
| `Settings` | 설정 메뉴 (escape 전용 닫기) |
| `Confirmation` | 권한 및 확인 대화상자 |
| `Tabs` | 탭 탐색 컴포넌트 |
| `Help` | 도움말 메뉴가 표시되는 경우 |
| `Transcript` | 트랜스크립트 뷰어 |
| `HistorySearch` | 기록 검색 모드 (Ctrl+R) |
| `Task` | 백그라운드 작업이 실행 중인 경우 |
| `ThemePicker` | 테마 선택기 대화상자 |
| `Attachments` | 이미지/첨부 파일 바 탐색 |
| `Footer` | 푸터 표시기 탐색 (작업, 팀, diff) |
| `MessageSelector` | 되감기 및 요약 대화상자 메시지 선택 |
| `DiffDialog` | Diff 뷰어 탐색 |
| `ModelPicker` | 모델 선택기 노력 수준 |
| `Select` | 일반 선택/목록 컴포넌트 |
| `Plugin` | 플러그인 대화상자 (탐색, 검색, 관리) |

## 사용 가능한 동작

동작은 `namespace:action` 형식을 따릅니다. 예를 들어 `chat:submit`은 메시지를 전송하고 `app:toggleTodos`는 작업 목록을 표시합니다. 각 컨텍스트에는 특정 동작이 사용 가능합니다.

### 앱 동작

`Global` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :--------------------- | :------ | :-------------------------- |
| `app:interrupt` | Ctrl+C | 현재 작업 취소 |
| `app:exit` | Ctrl+D | Claude Code 종료 |
| `app:toggleTodos` | Ctrl+T | 작업 목록 표시 전환 |
| `app:toggleTranscript` | Ctrl+O | 상세 트랜스크립트 전환 |

### 기록 동작

명령 기록 탐색을 위한 동작:

| 동작 | 기본값 | 설명 |
| :----------------- | :------ | :-------------------- |
| `history:search` | Ctrl+R | 기록 검색 열기 |
| `history:previous` | Up | 이전 기록 항목 |
| `history:next` | Down | 다음 기록 항목 |

### 채팅 동작

`Chat` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :-------------------- | :------------------------ | :----------------------- |
| `chat:cancel` | Escape | 현재 입력 취소 |
| `chat:cycleMode` | Shift+Tab* | 권한 모드 순환 |
| `chat:modelPicker` | Cmd+P / Meta+P | 모델 선택기 열기 |
| `chat:thinkingToggle` | Cmd+T / Meta+T | 확장 사고 전환 |
| `chat:submit` | Enter | 메시지 전송 |
| `chat:undo` | Ctrl+\_ | 마지막 동작 실행 취소 |
| `chat:externalEditor` | Ctrl+G | 외부 에디터에서 열기 |
| `chat:stash` | Ctrl+S | 현재 프롬프트 임시 저장 |
| `chat:imagePaste` | Ctrl+V (Windows에서는 Alt+V) | 이미지 붙여넣기 |

*VT 모드가 없는 Windows (Node &lt;24.2.0/&lt;22.17.0, Bun &lt;1.2.23)에서는 기본값이 Meta+M입니다.

### 자동 완성 동작

`Autocomplete` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :---------------------- | :------ | :------------------ |
| `autocomplete:accept` | Tab | 제안 수락 |
| `autocomplete:dismiss` | Escape | 메뉴 닫기 |
| `autocomplete:previous` | Up | 이전 제안 |
| `autocomplete:next` | Down | 다음 제안 |

### 확인 동작

`Confirmation` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :--------------------------- | :-------- | :---------------------------- |
| `confirm:yes` | Y, Enter | 동작 확인 |
| `confirm:no` | N, Escape | 동작 거부 |
| `confirm:previous` | Up | 이전 옵션 |
| `confirm:next` | Down | 다음 옵션 |
| `confirm:nextField` | Tab | 다음 필드 |
| `confirm:previousField` | (unbound) | 이전 필드 |
| `confirm:cycleMode` | Shift+Tab | 권한 모드 순환 |
| `confirm:toggleExplanation` | Ctrl+E | 권한 설명 전환 |

### 권한 동작

권한 대화상자의 `Confirmation` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :----------------------- | :------ | :--------------------------- |
| `permission:toggleDebug` | Ctrl+D | 권한 디버그 정보 전환 |

### 트랜스크립트 동작

`Transcript` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :------------------------ | :------------- | :----------------------- |
| `transcript:toggleShowAll` | Ctrl+E | 모든 내용 표시 전환 |
| `transcript:exit` | Ctrl+C, Escape | 트랜스크립트 뷰 종료 |

### 기록 검색 동작

`HistorySearch` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :---------------------- | :---------- | :----------------------- |
| `historySearch:next` | Ctrl+R | 다음 일치 항목 |
| `historySearch:accept` | Escape, Tab | 선택 수락 |
| `historySearch:cancel` | Ctrl+C | 검색 취소 |
| `historySearch:execute` | Enter | 선택한 명령 실행 |

### 작업 동작

`Task` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :---------------- | :------ | :----------------------- |
| `task:background` | Ctrl+B | 현재 작업 백그라운드로 이동 |

### 테마 동작

`ThemePicker` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :------------------------------- | :------ | :-------------------------- |
| `theme:toggleSyntaxHighlighting` | Ctrl+T | 구문 강조 전환 |

### 도움말 동작

`Help` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :------------- | :------ | :--------------- |
| `help:dismiss` | Escape | 도움말 메뉴 닫기 |

### 탭 동작

`Tabs` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :-------------- | :-------------- | :----------- |
| `tabs:next` | Tab, Right | 다음 탭 |
| `tabs:previous` | Shift+Tab, Left | 이전 탭 |

### 첨부 파일 동작

`Attachments` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :--------------------- | :---------------- | :-------------------------- |
| `attachments:next` | Right | 다음 첨부 파일 |
| `attachments:previous` | Left | 이전 첨부 파일 |
| `attachments:remove` | Backspace, Delete | 선택한 첨부 파일 제거 |
| `attachments:exit` | Down, Escape | 첨부 파일 바 종료 |

### 푸터 동작

`Footer` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :---------------------- | :------ | :------------------------ |
| `footer:next` | Right | 다음 푸터 항목 |
| `footer:previous` | Left | 이전 푸터 항목 |
| `footer:openSelected` | Enter | 선택한 푸터 항목 열기 |
| `footer:clearSelection` | Escape | 푸터 선택 해제 |

### 메시지 선택기 동작

`MessageSelector` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :----------------------- | :---------------------------------------- | :---------------- |
| `messageSelector:up` | Up, K | 목록에서 위로 이동 |
| `messageSelector:down` | Down, J | 목록에서 아래로 이동 |
| `messageSelector:top` | Ctrl+Up, Shift+Up, Meta+Up, Shift+K | 맨 위로 이동 |
| `messageSelector:bottom` | Ctrl+Down, Shift+Down, Meta+Down, Shift+J | 맨 아래로 이동 |
| `messageSelector:select` | Enter | 메시지 선택 |

### Diff 동작

`DiffDialog` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :-------------------- | :----------------- | :---------------------- |
| `diff:dismiss` | Escape | diff 뷰어 닫기 |
| `diff:previousSource` | Left | 이전 diff 소스 |
| `diff:nextSource` | Right | 다음 diff 소스 |
| `diff:previousFile` | Up | diff의 이전 파일 |
| `diff:nextFile` | Down | diff의 다음 파일 |
| `diff:viewDetails` | Enter | diff 세부 정보 보기 |
| `diff:back` | (컨텍스트별) | diff 뷰어에서 뒤로 이동 |

### 모델 선택기 동작

`ModelPicker` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :--------------------------- | :------ | :-------------------- |
| `modelPicker:decreaseEffort` | Left | 노력 수준 감소 |
| `modelPicker:increaseEffort` | Right | 노력 수준 증가 |

### 선택 동작

`Select` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :---------------- | :-------------- | :--------------- |
| `select:next` | Down, J, Ctrl+N | 다음 옵션 |
| `select:previous` | Up, K, Ctrl+P | 이전 옵션 |
| `select:accept` | Enter | 선택 수락 |
| `select:cancel` | Escape | 선택 취소 |

### 플러그인 동작

`Plugin` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :-------------- | :------ | :------------------------- |
| `plugin:toggle` | Space | 플러그인 선택 전환 |
| `plugin:install` | I | 선택한 플러그인 설치 |

### 설정 동작

`Settings` 컨텍스트에서 사용 가능한 동작:

| 동작 | 기본값 | 설명 |
| :---------------- | :------ | :---------------------------------- |
| `settings:search` | / | 검색 모드 진입 |
| `settings:retry` | R | 사용량 데이터 로드 재시도 (오류 시) |

## 키 입력 구문

### 수정자 키

`+` 구분자와 함께 수정자 키를 사용합니다:

* `ctrl` 또는 `control` - Control 키
* `alt`, `opt`, 또는 `option` - Alt/Option 키
* `shift` - Shift 키
* `meta`, `cmd`, 또는 `command` - Meta/Command 키

예시:

```
ctrl+k          단일 키 + 수정자
shift+tab       Shift + Tab
meta+p          Command/Meta + P
ctrl+shift+c    여러 수정자
```

### 대문자

단독 대문자는 Shift를 의미합니다. 예를 들어 `K`는 `shift+k`와 동일합니다. 이는 대문자와 소문자 키의 의미가 다른 vim 스타일 바인딩에 유용합니다.

수정자가 있는 대문자(예: `ctrl+K`)는 스타일 차이로 간주되며 Shift를 의미하지 **않습니다** - `ctrl+K`는 `ctrl+k`와 동일합니다.

### 코드

코드는 공백으로 구분된 키 입력 시퀀스입니다:

```
ctrl+k ctrl+s   Ctrl+K를 누르고 떼고, 그 다음 Ctrl+S
```

### 특수 키

* `escape` 또는 `esc` - Escape 키
* `enter` 또는 `return` - Enter 키
* `tab` - Tab 키
* `space` - 스페이스바
* `up`, `down`, `left`, `right` - 화살표 키
* `backspace`, `delete` - 삭제 키

## 기본 단축키 바인딩 해제

동작을 `null`로 설정하면 기본 단축키를 바인딩 해제합니다:

```json
{
  "bindings": [
    {
      "context": "Chat",
      "bindings": {
        "ctrl+s": null
      }
    }
  ]
}
```

## 예약된 단축키

다음 단축키는 재바인딩할 수 없습니다:

| 단축키 | 이유 |
| :------- | :------------------------- |
| Ctrl+C | 하드코딩된 인터럽트/취소 |
| Ctrl+D | 하드코딩된 종료 |

## 터미널 충돌

일부 단축키는 터미널 멀티플렉서와 충돌할 수 있습니다:

| 단축키 | 충돌 |
| :------- | :-------------------------------- |
| Ctrl+B | tmux 접두사 (두 번 눌러 전송) |
| Ctrl+A | GNU screen 접두사 |
| Ctrl+Z | Unix 프로세스 일시 중단 (SIGTSTP) |

## Vim 모드 상호 작용

vim 모드가 활성화된 경우(`/vim`), 키바인딩과 vim 모드는 독립적으로 작동합니다:

* **Vim 모드**는 텍스트 입력 수준에서 입력을 처리합니다 (커서 이동, 모드, 모션)
* **키바인딩**은 컴포넌트 수준에서 동작을 처리합니다 (todo 전환, 전송 등)
* vim 모드의 Escape 키는 INSERT에서 NORMAL 모드로 전환합니다; `chat:cancel`을 트리거하지 않습니다
* 대부분의 Ctrl+키 단축키는 vim 모드를 통과하여 키바인딩 시스템으로 전달됩니다
* vim NORMAL 모드에서 `?`는 도움말 메뉴를 표시합니다 (vim 동작)

## 유효성 검사

Claude Code는 키바인딩의 유효성을 검사하고 다음에 대한 경고를 표시합니다:

* 파싱 오류 (잘못된 JSON 또는 구조)
* 잘못된 컨텍스트 이름
* 예약된 단축키 충돌
* 터미널 멀티플렉서 충돌
* 동일한 컨텍스트에서의 중복 바인딩

`/doctor`를 실행하면 키바인딩 경고를 확인할 수 있습니다.
